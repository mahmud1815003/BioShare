import { createSlice } from "@reduxjs/toolkit";


const initialState = {};

const instituteSlice = createSlice({
    name: 'institute',
    initialState: initialState,
    reducers: {

    }
});

export default instituteSlice;